

pub async fn boot(width: u32, height: u32) {
    // 绘制沙子（Species.Sand）
    // for x in (5..=width - 5).step_by(10) {
    //     let y = (height - 40 + (5.0 * (x as f64 / 20.0).sin()) as i32) as u32;
    //     // window_u_paint(x, y, (Math::random() * 6.0 + 10.0) as u32, Species::Sand);
    //     // 模拟绘制过程中的延迟
    //     sleep(16).await;
    // }
    //
    // // 绘制种子（Species.Seed）
    // for x in (40..=width - 40).step_by(50 + (Math::random() * 10.0) as u32) {
    //     let y = (height / 2 + (20.0 * (x as f64 / 20.0).sin())) as u32;
    //     // window_u_paint(x, y, 6, Species::Seed);
    //     sleep(180).await;
    // }
}
